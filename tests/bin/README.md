# README for Bin Scripts

This folder contains CLI entry points and test scripts for the block theme scaffold.

- Place all CLI scripts and their tests here (e.g., build.js, build.test.js, generate-theme.js, generate-theme.test.js).
- Keep CLI logic focused on orchestration; move reusable helpers to `scripts/utils/`.

See the main scripts README for more details.
